# GPS 경로 좌표 도구

네이버 지도에서 폴리라인 경로를 그리고, 지정한 거리 간격마다 GPS 좌표를 생성해 CSV로 내려받는 웹 도구입니다.

현재 버전은 네이버 지도 API 등록을 위한 최초 공개 주소를 확보하는 기본 화면입니다. 배포 후 네이버 클라우드 플랫폼에서 Web Dynamic Map Client ID를 발급받아 지도와 경로 계산 기능을 연결합니다.

## 로컬 실행

```bash
pnpm install
pnpm dev
```

## 빌드

```bash
pnpm build
```
